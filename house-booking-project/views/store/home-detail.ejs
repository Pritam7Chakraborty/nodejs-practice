<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head') %>
</head>

<body class="bg-gray-900 text-gray-100">
  <%- include('../partials/nav') %>

  <main class="container mx-auto mt-20 px-4">
    <h2 class="text-5xl font-bold text-red-500 mb-6 text-center">
      Home Details
    </h2>
    <p class="text-2xl text-gray-300 mb-10 text-center">
      You are in the home detail page
    </p>

    <div class="bg-gray-800 rounded-xl shadow-lg p-6">

      <div class="relative w-full rounded-xl overflow-hidden min-w-[250px] mb-8">
        <img src="<%= home.photo %>" alt="House Image" class="object-cover w-full h-auto" />

        <div class="absolute bottom-0 left-0 right-0 p-4 bg-black bg-opacity-50 text-white">
          <h3 class="text-2xl md:text-3xl font-bold mb-1">
            <%= home.houseName %>
          </h3>
          <div class="flex items-center text-lg md:text-xl">
            <span class="text-yellow-400">⭐</span>
            <span class="ml-1 text-gray-200">
              <%= home.rating ? home.rating : 'N/A' %>/5
            </span>
            <span class="text-gray-400 ml-4">📍 <%= home.location %></span>
          </div>
        </div>
      </div>

      <div class="w-full flex flex-col justify-between">
        <div>
          <p class="text-gray-300 mb-4 text-lg">
            💰 <strong>Price:</strong>
            <span class="text-green-400 font-bold">₹<%= home.price %></span> / night
          </p>
          <div class="mt-4">
            <h4 class="text-2xl font-semibold mb-2">House Rules</h4>
            <a href="/rules/<%= home._id %>" class="inline-flex items-center text-blue-600 hover:text-blue-800">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-2-2m2 2l2-2m-2-2V8m0 0a1 1 0 011 1v3a1 1 0 01-1 1h-3a1 1 0 01-1-1V9a1 1 0 011-1h3zm0-4a2 2 0 11-4 0 2 2 0 014 0zm0 16a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
              Download House Rules
            </a>
          </div>
        </div>

        <div class="flex flex-col md:flex-row md:space-x-4 flex-wrap mt-6">
          <%- include('../partials/favourite') %>
          <button type="button" class="w-full flex-1 bg-gradient-to-r from-green-600 to-green-800 text-white p-2 text-sm md:text-base rounded-lg font-semibold shadow-md 
                   hover:from-green-700 hover:to-green-900 hover:shadow-lg 
                   transition duration-300 transform hover:scale-105
                   focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 focus:ring-offset-gray-900">
            Book Now
          </button>
        </div>
      </div>

    </div>
  </main>
</body>

</html>