<%- include('../partials/head') %>
</head>

<body class="bg-gray-900 text-gray-100">
  <%- include('../partials/nav') %>

  <main class="container mx-auto mt-20 px-4">
    <!-- Heading -->
    <h2 class="text-5xl font-bold text-red-500 mb-6 text-center">
      Home Details
    </h2>
    <p class="text-2xl text-gray-300 mb-10 text-center">
      You are in the home detail page
    </p>

    <!-- Amazon Style Layout -->
    <div class="bg-gray-800 rounded-xl shadow-lg p-6 flex flex-col md:flex-row md:items-start md:space-x-8">
      
      <!-- Left: Image -->
      <div class="w-full md:w-1/2 flex justify-center min-w-[250px]">
        <img src="<%= home.photo %>" alt="House Image"
          class="rounded-lg object-cover w-full max-w-lg h-auto transform hover:scale-105 transition duration-500 shadow-md" />
      </div>

      <!-- Right: Details -->
      <div class="w-full md:w-1/2 flex flex-col justify-between mt-6 md:mt-0">
        
        <!-- Info -->
        <div>
          <h3 class="text-3xl font-bold text-gray-100 mb-3">
            <%= home.houseName %>
          </h3>
          <p class="text-gray-400 mb-4 text-lg">
            📍 <%= home.location %>
          </p>
          <p class="text-gray-300 mb-4 text-lg">
            💰 <strong>Price:</strong> 
            <span class="text-green-400 font-bold">₹<%= home.price %></span> / night
          </p>
          <div class="flex items-center text-lg text-yellow-400 mb-6">
            ⭐ <span class="ml-1 text-gray-200">
              <%= home.rating ? home.rating : 'N/A' %>/5
            </span>
          </div>
        </div><div class="flex flex-row gap-4 flex-wrap mt-2">
          <%- include('../partials/favourite') %>

        <!-- Book Button -->
        
          <button type="button"
            class="bg-green-600 text-white p-2 text-small rounded-lg font-semibold shadow-md hover:bg-green-700 hover:shadow-lg transition duration-300 w-25">
            Book Now
          </button>
        </div>
        
      </div>

    </div>
  </main>
</body>
</html>
